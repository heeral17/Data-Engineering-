# Automated ETL Pipeline

## Overview
This project implements a lightweight ETL (Extract, Transform, Load) pipeline that processes raw CSV data, performs data cleaning, and stores structured output in a SQL database.

## Features
- Data Cleaning: Removes duplicates and handles missing values
- SQL Integration: Uses SQLAlchemy for database operations
- Automation: Supports scheduled execution

## Project Structure
